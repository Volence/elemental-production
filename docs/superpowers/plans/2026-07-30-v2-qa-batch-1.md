# Broadcast v2 — Owner QA Batch 1

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Land the owner's first OBS QA feedback round on the v2 package (pre-release, master at 0a864e5, v1.4.0 staged): brand-color crests, gray-logo team color, map-board/HUD polish, scoreboard title bug, between-matches recentering, interview caster slots, Ban Reveal OBS scene, collection settings-carryover, dashboard clear button.

**Constraints:** all prior v2 invariants hold (ES5 shared files / ES2019 scenes, no `?.`/`??`, proxied images, full-shorthand render animations, cam rects ONLY in cam-layout.js + regenerate collection after changes, mask helpers shared). NO push/tag. Screenshots verify every visual change.

---

### Task 1: Overlay + extraction polish (owner items 1–5, bans resolution)

**Files:** `overlays/pinwheel.js` (+test), `src/lib/color-extract.js` (+test), `overlays/starting-soon.html`, `overlays/brb.html`, `overlays/end-of-stream.html`, `overlays/series-winner.html` (waiting-state crest), `overlays/map-pick.html`, `overlays/gameplay-hud.html`, `overlays/casters.html`, `overlays/casters-scoreboard.html`, `overlays/casters-lobby.html`, `overlays/casters-map-score.html`, `overlays/casters-flythrough-hud.html`, `server/server.js` (getActiveBanIdx — read first)

- [x] **1a. pinwheelSVG brand mode.** Add optional `opts.colors: [c1,c2,c3,c4]` — when present, petal pairs map to the 4 colors in visual order matching the real elmt.gg logo (petals are ordered p,p,s,s,p,p,s,s; assign pairs [0..1]→c1, [2..3]→c2, [4..5]→c3, [6..7]→c4 and VERIFY against the reference logo screenshot the owner supplied — red/gold/green/blue quadrants; adjust pair→color assignment until the rendered SVG visually matches, screenshot proof). color1/color2 behavior unchanged when `colors` absent. Test: 4 distinct stroke colors ×2 each.
- [x] **1b. Neutral crests use brand colors.** starting-soon, brb, end-of-stream crests + series-winner waiting-state crest + any caster-scene crest NOT flanked by team chips → `pinwheelSVG({colors: [--elmt-red, --elmt-gold, --elmt-green, --elmt-blue] resolved via getComputedStyle or hardcoded hsl literals matching theme-v2})`. Match-context medallions (HUD, map-pick header, map-intro, casters matchup deck hub) KEEP team colors.
- [x] **1c. Gray-logo fallback.** In color-extract.js: when every bucket fails the saturation gate but buckets exist, return a NEUTRAL derived from the dominant gray bucket — average its rgb, clamp lightness into a legible band (min ~#8a8a8a, max ~#c8c8c8), return that instead of ELMT_ACCENT_FALLBACK. Keep ELMT_ACCENT_FALLBACK only for the no-buckets/error paths. Tests: gray-heavy logo → silver in band (not blue); error path still accent.
- [x] **1d. Map-board pills.** Remove the `· WON` suffix entirely; pill font 22px → 18px (owner: slightly smaller; still legible — this is a deliberate floor exception for a redundant label, note it).
- [x] **1e. HUD center band tidy.** Remove the map pill (`.v2-center-map` name); keep event pill + medallion + pips; re-center spacing.
- [x] **1f. Remove "CASTED BY"** subtitle from all caster scenes' eventHeader calls.
- [x] **1g. Scoreboard title.** Replace `round.mapName || round.map` with the state map name: resolve `state.maps[idx].name` for the displayed round idx, else 'Match Stats'. NEVER print round.map (FACEIT hex id — the 0X0800… bug).
- [x] **1h. Bans on finished matches.** Read `getActiveBanIdx` in server.js: make heroBans resolve current-OR-last-played (findCurrentMapIndex semantics) so a finished match still exposes the last map's bans; map-board live-column bans → show on the current-or-last column (white glow stays current-only). Verify with a completed-series seed: HUD wings + map board bans render.
- [x] **1i. Verify:** screenshots (starting-soon crest = 4-color brand; map board pills; HUD without map pill; scoreboard title with real name; bans on finished seed). Full `npm test` + vite build. Commit.

### Task 2: cam-layout changes + interview slots + Ban Reveal scene + settings carryover

**Files:** `overlays/cam-layout.js` (+test), `overlays/between-matches.html`, `overlays/interview.html`, `scripts/build-scene-collection-v2.mjs` (+test), `data/obs-scene-collection*.json` (regenerated), `docs/scene-collection-v2-migration.md`

- [x] **2a. Between-matches recenter.** wide.single → centered: `{ x: 115, y: 84, w: 1690, h: 860 }` (equal 115px side margins; y pushed below the header band so the event header never overlaps the window; verify header fully clear + text not clipped — fix the header max-width/ellipsis so nothing looks cut). Scene CSS: schedule/countdown band adjusts to the new bottom edge.
- [x] **2b. Interview caster slots.** cam-layout interview group gains `casters: [ {x:60,y:760,w:420,h:262}, {x:1440,y:760,w:420,h:262} ]` (bottom corners, clear of the player card). interview.html renders camFrames for them (pills = caster names from state.casters) when casters are named; cutout mask includes their rects. Collection: bake Caster 1/Caster 2 into the Interview scene at those rects (generator mapping addition).
- [x] **2c. Ban Reveal OBS scene.** Generator gains an ensure-scene step: if no "Ban Reveal" scene exists, create it (scene source + a "Ban Reveal BS" browser source at http://localhost:3001/overlays/hero-bans.html 1920×1080 + scene_order entry after "Map Pick"). Idempotent. Both JSONs.
- [x] **2d. Settings carryover.** Generator flag `--carry-from <path>` (an existing OBS scene-collection JSON, e.g. the producer's live one in ~/.config/obs-studio/basic/scenes/): copies `settings.url` for the cam browser sources and `settings.local_file`/`playlist` for media sources (Background Music, Casters Background Music, Map Flythrough, Map Music, Replay) BY SOURCE NAME into the v2 files. Never overwrites a non-empty v2 value with an empty carry value. Migration note updated (how to locate the current collection file; carryover usage; re-import steps).
- [x] **2e. Regenerate, tests (new rects, ensure-scene idempotency, carryover unit test with a fixture), screenshots (between-matches centered + header clear; interview with corner slots over gray). Commit.**

### Task 3: Dashboard clear button + wrap-up

**Files:** dashboard component owning the Interview/Guest Cam panel (find it — likely `src/pages/ProductionControls.jsx` or MatchHub), `CHANGELOG.md`

- [x] **3a. Quick Clear.** A "Clear" button on the Interview/Guest Cam panel: one click PATCHes interviewee to `{name:'',camUrl:'',visible:false, teamName:'',teamLogo:'',teamColor:'',role:'',label:''}` (match the actual field set the panel writes). Use ConfirmModal ONLY if other destructive buttons in this panel do; otherwise instant (it's cheap to re-enter). Match the panel's existing button styling.
- [x] **3b. Wrap-up:** full test suite + vite build; serve sweep of touched scenes; CHANGELOG addendum under v1.4.0 ("owner QA batch 1"); commit. NO version bump beyond 1.4.0, NO push/tag.
