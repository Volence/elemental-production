# Bug Batch 2 (audit-found) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix the live bugs surfaced by the 2026-07-29 code-health audit (`docs/superpowers/specs/2026-07-29-code-health-audit.md` §A) before the redesign. Ships as v1.3.8.

**Architecture:** Same stack as batch 1 (Express `server/server.js`, React dashboard `src/pages`, static overlays `overlays/`). Fixes are grouped by layer into three implementation tasks + wrap-up. Audit reports (with exact line refs) live in `docs/superpowers/specs/audit-appendices/` — implementers MUST read the referenced audit entry before each fix and adapt to the real code (line numbers drift).

**Tech Stack:** Node/Express (ESM), React, vanilla ES5-flavored overlays, vitest (`npm test`, currently 13 passing).

**Out of scope (redesign foundation handles):** A8 HUD bar replay (needs render-vs-reflow split), the 8MB ELMT_BG inlining, the image-inlining regex hardening, overlay runtime unification, server decomposition.

---

### Task 1: Server fixes (A1-server, A5, A7, A11, + proxied team logos)

**Files:** Modify `server/server.js`, `server/faceit-merge.js` (callers only), `server/state.js` if needed. Test: extend `server/faceit-merge.test.js` where logic is extractable.

- [ ] **Step 1 — A1 eventName override guards (server side).** All FACEIT→state paths that write `eventName` (loader `/api/faceit/match` ~line 797 `if (details.competitionName) update.eventName = ...`; check `/api/faceit/refresh` and `faceitPollTick` for their own eventName writes — audit says at least one syncs it every 15s) must guard with `if (!isOverridden('eventName'))`:

```js
    if (details.competitionName && !isOverridden('eventName')) update.eventName = details.competitionName;
```

- [ ] **Step 2 — A5 BRB timer stale startedAt.** In `/api/brb` (~line 1877), the partial countdown update omits `startedAt` (and `remaining`), so deep-merge keeps stale values. Make it set the full running-timer shape exactly like `/api/timer/start` does:

```js
    countdown: {
      ...getState().countdown,
      duration: d,
      remaining: d,
      running: true,
      startedAt: Date.now(),
      target: null,
    },
```
(Read the actual handler; keep its label/scene behavior; only complete the countdown fields.)

- [ ] **Step 3 — A7 auto-sync resumes on boot.** In the startup section (~2052+, after `loadState()`): if `getState().faceitAutoSync && getState().faceitMatchId`, call `startFaceitPoll()` so the persisted "Auto Sync ON" is true again after a restart. Guard against double-start (startFaceitPoll already checks the interval).

- [ ] **Step 4 — A11 reset stops the poll.** In `/api/reset` (~1726), call `stopFaceitPoll('reset')` (match the existing stop-reason style) before/with the state reset, and clear `faceitAutoSync: false` in the reset data if the handler doesn't already.

- [ ] **Step 5 — A6 server half: score quick-actions set overrides.** `/api/score/increment` (~1673) and `/api/map-win` (~1773) mutate `teams.*.score` (and maps) without overrides, so the poll reverts them in FACEIT mode. In each handler, when `getState().mode === 'faceit' && getState().faceitMatchId`, also `setOverrides(['teams.team1.score','teams.team2.score'])` (and for `/api/map-win`, since it edits maps' winner which feeds computedScore — read `buildMapsUpdate`'s forward-only logic first: if the map-winner path already survives via forward-only merge, only the score overrides are needed; document your conclusion in the commit body).

- [ ] **Step 6 — proxied team logos.** Loader (~827-838 region) and poll caller of `buildTeamsUpdate` currently store `faction.avatar` raw. Route through the existing `proxyImageUrl` helper (used for heroes ~1580) at the CALLER level (keep `buildTeamsUpdate` pure — pass already-proxied avatar values in, e.g. `faction1: { ...faction1, avatar: proxyImageUrl(faction1.avatar) }`). Check `proxyImageUrl`'s signature first (sync? returns local URL?) and follow how heroes use it. Add one vitest case asserting buildTeamsUpdate passes through whatever logo value it's given (no proxying inside the pure fn).

- [ ] **Step 7 — verify + commit.** `npm test` (all green, incl. new case), boot check `timeout 6 node server/server.js` (exit 124), eslint clean on touched-file baseline. Commit: `fix: server audit batch — eventName guard, BRB timer, poll lifecycle, score overrides, proxied team logos`.

---

### Task 2: Dashboard fixes (A1-dashboard, A6-dashboard, A9, A12)

**Files:** Modify `src/pages/MatchHub.jsx`, `src/pages/ProductionControls.jsx`, `src/pages/Theming.jsx`.

- [ ] **Step 1 — A1 dashboard half.** The two `eventName` PATCH sites in MatchHub (~343 manual-mode field, ~478 faceit-mode field — read them) must set the override in FACEIT mode, following the existing `overrideField` pattern:

```jsx
    // faceit mode: lock the field so auto-sync stops overwriting it
    onChange={e => (state.mode === 'faceit' && state.faceitMatchId)
      ? overrideField('eventName', { eventName: e.target.value })
      : updateState({ eventName: e.target.value })}
```
(Adapt to the actual JSX; if both sites already funnel through one helper, change the helper.)

- [ ] **Step 2 — A6 dashboard half.** MatchHub map Win/undo/swap-winner handlers (~708-751) adjust `teams.*.score` without overrides. In FACEIT mode add `setOverride('teams.team1.score', 'teams.team2.score')` alongside the score-changing updateState (setOverride accepts multiple paths — verify its signature at ~94).

- [ ] **Step 3 — A9 camDebounceRef.** ProductionControls ~101: `const camDebounceRef = {};` is recreated every render — the debounce never works. Fix:

```jsx
  const camDebounceRef = useRef({});
```
and use `camDebounceRef.current[index]` in `updateCasterCam`. Verify `useRef` is imported.

- [ ] **Step 4 — A12 theme reset clears color locks.** Theming `resetToDefault` (~91): after resetting the theme, clear the `teams.team1.color`/`teams.team2.color` overrides (same fetch pattern `saveTheme` uses for clearing, ~64-78).

- [ ] **Step 5 — verify + commit.** `npm test`, eslint baselines on all three files. Commit: `fix: dashboard audit batch — eventName/score overrides, caster cam debounce, theme reset unlocks colors`.

---

### Task 3: Overlay fixes (A2, A3, A4, A10, renderKey gaps)

**Files:** Modify `overlays/hero-bans.html`, `overlays/casters-scoreboard.html`, `overlays/starting-soon.html`, `overlays/interview.html`, `overlays/casters-lobby.html`, `overlays/between-matches.html`. Overlay JS must stay ES5-flavored; no top-level import/export.

- [ ] **Step 1 — A2 hero-bans blank-forever.** `hero-bans.html:96-102`: `loadHeroes` lacks `.catch`. Align it with the pattern the other 5 overlays use (read gameplay-hud.html:291 and copy that exact resilient form: `window.__HERO_DATA__ || await fetch(...).then(r => r.json()).catch(() => [])`), so a failed fetch yields `[]` and the overlay still registers `stateSync(update)`.

- [ ] **Step 2 — A3 dead map buttons.** `casters-scoreboard.html` `switchMap` (~444) mutates local `currentMap` but never re-renders. After setting it: `lastRenderKey = null; update(lastState);` — read how the file stores the last state (if it doesn't keep one, mirror how other overlays trigger re-render, or include `currentMap` in the renderKey and force a poll-tick repaint; pick the smallest correct mechanism and explain it in the commit body).

- [ ] **Step 3 — A4 unproxied logos.** `starting-soon.html:227,233` → wrap in `proxyImg(...)` (the same file already does this at :243,:249 — match that). `interview.html:131` → `proxyImg(teamLogo)`.

- [ ] **Step 4 — A10 renderKey latch.** `casters-scoreboard.html`: the early return for malformed stats (~284) happens AFTER `lastRenderKey` was set (~233). Move the validity check before the key assignment, or clear `lastRenderKey` on the early-return path, so a corrected frame re-renders.

- [ ] **Step 5 — renderKey gaps.** `casters-lobby.html` (~238-241): add `state.perMapBans` to the renderKey (it's read at ~254/267); optionally drop unused `state.schedule` from the key. `between-matches.html` buildKey: include schedule `team1Logo`/`team2Logo`. `starting-soon.html` buildKey: include schedule `time`/`label` fields. Keep each key minimal — only what the render reads.

- [ ] **Step 6 — verify + commit.** `npm test`; serve check: `PORT=3999 node server/server.js` + curl each touched overlay returns 200 and contains the edits; kill server. Confirm no `?.`/`import`/`export`/arrow syntax added to shared files (inline overlay scripts may keep their existing style). Commit: `fix: overlay audit batch — hero-bans resilience, scoreboard interactivity, proxied logos, renderKey gaps`.

---

### Task 4: Wrap-up

- [ ] **Step 1.** Full `npm test` + boot check + eslint sweep over all touched files (baseline-diff only).
- [ ] **Step 2.** Bump `package.json` version `1.3.7` → `1.3.8`.
- [ ] **Step 3.** Commit: `fix: audit-found bug batch — event name clobber, blank hero-bans, dead scoreboard buttons, unproxied logos, timer/poll lifecycle; bump to 1.3.8`.
