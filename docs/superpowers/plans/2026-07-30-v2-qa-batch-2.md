# Broadcast v2 — Owner QA Batch 2

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox syntax.

**Goal:** Second owner OBS QA round: heroBans derived-state consistency (the "bans everywhere are empty" root cause), Ban Reveal never-black, map-board bans on all played columns (no name captions), map-intro composition redesign, flythrough extended content, wider lobby/scoreboard panels, package-wide subtle idle animations.

**Constraints:** all v2 invariants hold (see prior plans). NO push/tag. Screenshots verify every visual change. Owner reference for HUD ban presentation density: the EWC26 screenshot (bans as small portraits adjacent to team plates — we already have wings; the problem is DATA, not layout).

---

### Task 1: heroBans consistency + Ban Reveal never-black + map-board all-column bans

**Files:** `server/server.js` (+ `server/faceit-merge.js` if the recompute helper fits there, + tests), `overlays/hero-bans.html`, `overlays/map-pick.html`

- [x] **1a. Root-cause + fix heroBans staleness.** `computeHeroBans` runs ONLY at FACEIT match-load and poll ticks (server.js ~834/~945). A persisted state (server restart, sync idle, match loaded pre-fix) keeps stale/empty `heroBans` forever — this is why the owner sees no bans in the HUD and a black Ban Reveal even though perMapBans is populated (scoreboard proves it). Make heroBans DERIVED-CONSISTENT: recompute from (perMapBans, maps, selectedMapIdx) at (1) server boot after state load, (2) every PATCH /api/state that touches maps/perMapBans/selectedMapIdx/banSwaps, (3) keep the existing sync-tick paths. One shared recompute call — no logic duplication. Respect the existing overrides mechanism (do not clobber a producer heroBans override if one exists — read how overrides gate computeHeroBans in the poll path and mirror it). Tests: boot-recompute (persisted perMapBans + empty heroBans → resolved), PATCH-recompute, override respected.
- [x] **1b. Ban Reveal never-black.** hero-bans.html currently hides EVERYTHING when both ban lists are empty (fine as an element overlay, wrong as a dedicated OBS scene — reads as dead air). New empty-state: petal texture + washes + crest + `HERO BANS` heading stay visible, panels area shows a subtle `AWAITING BANS` chip (≥28px, dimmed). The hidden-mode CSS class goes away; visibility is now the producer's scene-switch decision. Verify: empty seed → texture+heading+chip (NOT black); bans seed → full reveal as before.
- [x] **1c. Map board: bans on ALL played columns, names dropped.** Every column whose perMapBans entry has team1Ban/team2Ban shows its two ban tiles — completed maps included. Current/last column keeps 84px tiles; completed columns use smaller 56px tiles. NO hero-name captions on any board tiles (owner: "we don't need the names") — banTile without heroName, or pass empty (check banTile renders no empty plate). Keep the tiles inside the footer area like the owner's screenshot shows for map 4 (above the map name), consistent across columns. Verify with the finished-series seed: all 4 played columns show slashed portraits, no names, no layout overflow (footer still fits — adjust column footer height once, consistently).
- [x] **1d. Verify + commit.** Screenshots: map board finished seed (bans everywhere, nameless), Ban Reveal empty + populated, HUD wings populated after a server RESTART against persisted state (prove 1a: seed state, restart server, screenshot without any PATCH). Full npm test + vite build. `fix: heroBans derived consistency, Ban Reveal never-black, board bans on all played maps`

### Task 2: Map-intro composition redesign + flythrough extended content

**Files:** `overlays/map-intro.html`, `overlays/casters-flythrough-hud.html`, `overlays/cam-layout.js` ONLY if flythrough rects must move (then regenerate collection), `data/obs-scene-collection*.json` (only if regenerated)

- [x] **2a. Map-intro redesign.** Owner verdict: top bar feels squished/linear, bottom bar mostly empty — and the flythrough background does NOT need to stay visible ("we can have anything in the middle or any space"). New composition (design judgment within the v2 language; keep the dark over-video gradient): a CENTERED hero block — large map name (≥72px Geist) + `MAP n · MODE` chip + MATCH POINT chip when applicable; team matchup row beneath (logos ≥64px, names ≥32px, series score around a pinwheel hub ≥56px, ATTACK FIRST / MAP PICK tags); ban art tiles (banArtTile, ~140px, nameless or named — judge) flanking or beneath. Drop the topFrame bar entirely from this scene (the HUD look doesn't serve it — full-screen beat like Ban Reveal). Staged entrances (slideUp/scaleIn, staggered, full inline shorthand). Keep key discipline (re-derive read-set honestly). Center composition must clearly outweigh empty space — fill the middle.
- [x] **2b. Flythrough extended.** casters-flythrough-hud gains map-intro-style info WITHOUT covering the cams: keep the two cam frames + event header; add beneath/between: `MAP n · NAME · MODE` chip row + compact matchup strip (logos, names, score hub, pips) centered below the cams (~y 620-720 band). Same data reads as map-intro's (key updated). It stays an over-video scene — no texture.
- [x] **2c. Verify + commit.** Screenshots both scenes (rich seed; flythrough over gray). Console clean. `feat: map-intro centered composition + flythrough info band`

### Task 3: Wider caster panels + idle-motion package + wrap-up

**Files:** `overlays/casters-lobby.html`, `overlays/casters-scoreboard.html`, `overlays/theme-v2.css`, the scenes that opt in, `CHANGELOG.md`

- [x] **3a. Wider panels.** Lobby + scoreboard deck panels get ~15-20% more width (owner: "a little wider for both"): scoreboard `.sb-panel` and lobby `.lb-panel` max-width up (measure current, add ~200px, keep within canvas margins + fitPanelToCanvas guard). Screenshots before/after.
- [x] **3b. Idle-motion package (spec §7 "idle life").** Shared theme-v2.css utilities: `.v2-idle-breathe` (scale 1→1.035→1, ~4s ease-in-out infinite — transform ONLY on dedicated inner elements, never on entrance-animated wrappers or #root children that carry `both` fills — document the rule), `.v2-idle-glow` (drop-shadow/opacity pulse ~5s), `.v2-idle-spin` (very slow rotation, ~45s linear, for pinwheel crests only). Apply tastefully: team logos in plates/decks (breathe), neutral crests on full-screens (slow spin + glow), live pip (glow pulse — check if it already pulses), series-winner keeps its own richer animation. Respect the existing invariants: no retained transforms on wrappers; idle elements must be leaf nodes whose transform doesn't fight entrance animations (wrap in a span if needed). ONE consistent implementation — no per-scene keyframe copies. Verify: screenshots can't show motion — verify via CDP getAnimations (each opted-in element reports the running idle animation) + confirm entrance replay still works (animation stacking: entrance on wrapper, idle on inner — test one scene's OBS-visibility replay path via the data-anim round-trip).
- [x] **3c. Interview transparency re-check** (owner asked): gray-body pixel test on interview.html — interviewee window + both caster corners transparent. (Expected already-true; document proof.)
- [x] **3d. Wrap-up:** full suite + vite build; serve sweep of all touched scenes; CHANGELOG "Owner QA batch 2" under v1.4.0; commit. NO version bump, NO push/tag.
